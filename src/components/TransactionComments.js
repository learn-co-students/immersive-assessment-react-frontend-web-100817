import React from "react";

const TransactionComments = props => {
  const { transaction } = props;
  return (
    <tr>
      <div> Hello from Transaction Comments </div>
    </tr>
  );
};

export default TransactionComments;
